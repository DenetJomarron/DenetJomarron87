@model PagedList.IPagedList<ProjectToShow.Models.PersonViewModel>
@using PagedList.Mvc;
<link href="~/Content/PagedList.css" rel="stylesheet" type="text/css" />

@{
    ViewBag.Title = "Person Infomation";
    var listPerson = ViewBag.list;
}

<h2>Person Information</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@*@using (Html.BeginForm("Index", "Person", FormMethod.Get))
{
   
}*@
<table class="table">
    <tr>
        <th>
            Name

        </th>
        <th>
            Last Name
        </th>
        <th>
            Age

        </th>
        <th>
            Phone
        </th>
        <th>
            Sex
        </th>
        <th></th>
    </tr>
    @if (listPerson.Count == 0)
    {
        <tr>
            <td>
                The
            </td>
            <td>
                person
            </td>
            <td>
                list
            </td>
            <td>
                is
            </td>
            <td>
                empty.
            </td>
            <td>
            </td>
        </tr>

    }
    else
    {

        foreach (var item in listPerson)
        {
            <tr>
                <td>
                    @item.Name
                </td>
                <td>
                    @item.LastName
                </td>
                <td>
                    @item.Age
                </td>
                <td>
                    @item.Phone
                </td>
                <td>
                    @item.Sexo
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }
    }
</table>
<br />
